<div id="follow_form">
  <% if current_user.following?(artist) %>
      <%= render partial: 'artists/unfollow', locals: {artist: artist} %>
  <% else %>
      <%= render partial: 'artists/follow', locals: {artist: artist} %>
  <% end %>
</div>
