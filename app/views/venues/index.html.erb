<% provide(:title, 'Upcoming Concerts') %>

<% if logged_in? && current_user.admin? %>
    <div class="add_venue">
      <%= button_to "Add new venue", new_venue_path, method: 'get', id: "new_venue_link", remote: true %>
    </div>
<% end %>

<div class="row" id="all_venues">
  <h1>Venues</h1>

  <div class="row">
    <%= form_tag venues_path, :method => 'get', :id => "venues_search" do %>
        <div id="search_hidden_fields">
          <%= render 'search_hidden_fields' %>
        </div>
        <div class="col-12">
          <div class="row">
            <div class="col-sm-5 col-sm-offset-2" style="display: flex; align-items: end;" >
              <%= text_field_tag :search, params[:search] %>
            </div>
            <div class="col-sm-3">
              <%= submit_tag "Search", name: nil, class: "btn btn-primary" %>
            </div>
          </div>
        </div>
    <% end %>
  </div>

  <div class="pagination_list row">
    <%= will_paginate %>
  </div>

  <div id="all_concerts_with_sort">
    <%= render partial: 'venues' %>
  </div>

  <div class="row">
    <%= will_paginate %>
  </div>

</div>

